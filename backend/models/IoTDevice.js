// IoTDevice model - using Supabase for storage
// IoT Devices are stored in Supabase PostgreSQL database
// Use Supabase client from '../config/supabase.js' in route handlers

const IoTDevice = {
  // Placeholder model - implement using Supabase in route handlers
  // Example: const { data, error } = await supabase.from('iot_devices').select('*')
};

export default IoTDevice;
