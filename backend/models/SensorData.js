// SensorData model - using Supabase for storage
// Sensor data is stored in Supabase PostgreSQL database
// Use Supabase client from '../config/supabase.js' in route handlers

const SensorData = {
  // Placeholder model - implement using Supabase in route handlers
  // Example: const { data, error } = await supabase.from('sensor_data').select('*')
};

export default SensorData;
