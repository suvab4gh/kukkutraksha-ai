// Farm model - using Supabase for storage
// Farms are stored in Supabase PostgreSQL database
// Use Supabase client from '../config/supabase.js' in route handlers

const Farm = {
  // Placeholder model - implement using Supabase in route handlers
  // Example: const { data, error } = await supabase.from('farms').select('*')
};

export default Farm;
